# identity-service'e özel konfigürasyon
server:
  port: 9001

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/identity_db
    username: telcox
    password: telcox
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
  data:
    redis:
      host: localhost
      port: 6379

springdoc:
  swagger-ui:
    path: /swagger-ui.html
